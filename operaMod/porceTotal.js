function porcentaje(a, b){
    return (a/b) * 100 + "%";//Numero decimales, c
};

/* function porcentaje2(a, b) {
    let porcertaje = (a/b) * 100;
    return Math.round(porcertaje) + "%"; //Numeros enteros.
};
*/

module.exports = porcentaje;